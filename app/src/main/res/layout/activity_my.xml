<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MyActivity"
    xmlns:alterego="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/beginning_tv"
        android:layout_centerInParent="true"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="START" />

    <com.alterego.progressbarlayout.ProgressBarLayoutView
        android:id="@+id/expanded_circle_progress"
        android:layout_centerInParent="true"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"

        alterego:textProgressSize="20sp"
        alterego:textProgressString="%d%% done"
        alterego:beginningProgressSize="40dp"
        alterego:textProgressColor="@android:color/black"
        alterego:progressCircleColor="#999999" />

    <TextView
        android:id="@+id/ending_tv"
        android:layout_centerInParent="true"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#666666"
        android:gravity="center"
        android:text="DONE!!!"
        android:visibility="gone" />

</RelativeLayout>
